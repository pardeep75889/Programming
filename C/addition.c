#include<stdio.h>
int main(){
    int a, b, c;

    printf("Enter the first no. : ");
    scanf("%d", &a);

    printf("Enter the second no. : ");
    scanf("%d", &b);

    c = a + b;

    printf("Sum of two no. : %d ", c);
    return 0;
}